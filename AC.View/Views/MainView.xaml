<!-- Copyright (c) Microsoft Corporation. All rights reserved. -->
<!-- Licensed under the MIT License. See LICENSE in the project root for license information. -->

<Window
    x:Class="AC.View.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AC.View.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid DataContext="{StaticResource MainViewModel}" Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="340"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <TextBlock>
                    <Run Text="Active Processes: " FontWeight="Bold"/>
                    <Run Text="{Binding ActiveProcessCount}"/>
                </TextBlock>
            </Grid>

            <ListView Grid.Row="1"
            BorderThickness="1"
            BorderBrush="{ThemeResource SystemControlForegroundBaseMediumLowBrush}"
            SelectionMode="None"
            ItemsSource="{Binding ActiveProcesses}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox MinWidth="0" VerticalAlignment="Center"/>
                            <TextBlock Margin="0,0,10,0" VerticalAlignment="Center">
                                <Run Text="ID: " FontWeight="Bold"/>
                                <Run Text="{Binding Id}"/>
                            </TextBlock>
                            <TextBlock Margin="0,0,10,0" VerticalAlignment="Center">
                                <Run Text="Name: " FontWeight="Bold"/>
                                <Run Text="{Binding Name}"/>                                
                                <Run Text=" - " FontWeight="Bold"/>
                                <Run Text="{Binding WindowTitle}"/>
                            </TextBlock>
                            <Button Content="Show" Command="{Binding Source={StaticResource MainViewModel}, Path=ShowProcessCommand}" CommandParameter="{Binding}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.ItemContainerTransitions>
                    <TransitionCollection/>
                </ListView.ItemContainerTransitions>
            </ListView>
        </Grid>
    </Grid>
</Window>
